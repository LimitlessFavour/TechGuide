<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.techguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LecturerDetailActivity"></activity>
        <activity android:name=".selectTeacher" />
        <activity android:name=".Lecturer" />
        <activity android:name=".aboutApp" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".modified_nav"
            android:label="@string/title_activity_modified_nav"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".order_pizza" />
        <activity android:name=".actvity_main_navigation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>