<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/original_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_main"
        app:menu="@menu/activity_main_drawer" />
    <!--the navigation view will contain an header layout most likely created somewhere else-->
    <!--and a menu which should appear below the header(new menu activty which should be different from the
    main menu.
    we see now that the app menu and app header layout are essential components of the navigation view
    Navigation view should be contained in the main activity.
    The next question that comes to mind is:does this new menu have to be inflated?
    Answer:
    .-->
    <!---->



</android.support.v4.widget.DrawerLayout>
